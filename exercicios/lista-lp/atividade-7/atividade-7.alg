algoritmo "semnome"
// Função : Exibir a idade total em dias
// Autor :
// Data : 07/03/2023
// Seção de Declarações 
var
a, m, d, idade : real
inicio
// Seção de Comandos
escreval("anos")
leia(A)
escreval("meses")
leia(m)
escreval("dias")
leia(d)
a := (a * 365)
m := (m * 30)
d := (d * 1)
idade := ( a + m + d)
escreval ("Idade total em dias ", idade)
fimalgoritmo
